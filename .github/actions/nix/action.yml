name: Nix
description: Install Nix and configure the shared Cachix binary cache

inputs:
  github-token:
    description: Token used to authenticate Nix's GitHub fetches
    required: false
    default: ""
  cachix-token:
    description: Cachix auth token; omit to use the cache read-only
    required: false
    default: ""

runs:
  using: composite
  steps:
    - uses: unmango/actions/setup-nix@3ec4ed5a6af9c00f6a39e079df488573c77ebda6 # v1.0.1
      with:
        github_access_token: ${{ inputs.github-token }}
        cachix_auth_token: ${{ inputs.cachix-token }}
